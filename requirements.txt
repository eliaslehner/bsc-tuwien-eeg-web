# ── Core Scientific Stack ─────────────────────────────────────────────────────
numpy                       # Array computing
matplotlib>=3.7             # Plotting and visualisation

# ── Neuroimaging ──────────────────────────────────────────────────────────────
mne==1.11.0                 # EEG/MEG processing
nilearn>=0.13.0             # Neuroimaging machine learning
nibabel>=5.3.3              # Neuroimaging file format support (NIfTI, GIFTI, etc.)
antspyx==0.6.3              # ANTs registration (subject<->MNI atlas warp); optional — pipeline falls back to affine-only resample if absent

# ── Image & 3D Processing ─────────────────────────────────────────────────────
scikit-image>=0.25.2        # General image processing
open3d>=0.19.0              # 3D data processing and visualisation
plyfile>=1.1.3              # PLY file I/O (ASCII and binary)

# ── PyTorch (CUDA 12.8) ───────────────────────────────────────────────────────
--extra-index-url https://download.pytorch.org/whl/cu128
torch
torchvision
torchaudio

# ── Utilities ─────────────────────────────────────────────────────────────────
psutil>=5.9.0               # System and process monitoring
python-dotenv>=1.0.0        # .env file support